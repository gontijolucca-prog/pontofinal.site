{
  "name": "pontofinal-ig-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Instagram DM bot (Alberto) for PontoFinal.site — Cloudflare Worker + OpenRouter/free + Supabase",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:set": "echo 'Run: wrangler secret put META_APP_SECRET (etc — see wrangler.toml)'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
